{
  "name": "game-discovery",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "app:build": "yarn workspace @game-discovery/app build",
    "app:codegen": "yarn workspace @game-discovery/app codegen",
    "app:lint": "yarn workspace @game-discovery/app lint",
    "app:start": "yarn workspace @game-discovery/app start",
    "clean": "rm -rf node_modules/ && rm -rf packages/*/node_modules/",
    "lint": "./scripts/lint.sh",
    "schema:codegen": "yarn workspace @game-discovery/schema codegen",
    "server:lint": "yarn workspace @game-discovery/server lint",
    "server:start": "yarn workspace @game-discovery/server start"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.31.0",
    "@typescript-eslint/parser": "^2.31.0",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^3.1.3",
    "eslint-plugin-react": "^7.19.0",
    "prettier": "^2.0.5",
    "typescript": "^3.8.3"
  }
}
