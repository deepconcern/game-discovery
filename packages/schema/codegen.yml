overwrite: true
schema: "../../schema.graphql"
generates:
  src/index.tsx:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-react-apollo"
  ./graphql.schema.json:
    plugins:
      - "introspection"
